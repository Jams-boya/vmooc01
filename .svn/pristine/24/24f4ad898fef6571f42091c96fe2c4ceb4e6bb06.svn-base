import {myRender} from './common';
import config from '../config';

export default {
  /**
   * 入口
   */
  entry(req, res, next) {
    myRender(req, res, 'demo/index', {title: config.name + '-首页', home: true});
  },




};